/root/projects/
├── build-docker/
│   ├── docker-compose.yml
│   └── nginx/
│       └── default.conf
├── smart-java-spring-final/
│   └── Dockerfile
├── smart-react-vite-final/
│   ├── dist/                ← build sẵn tại local
│   └── ...
└── upload-docker/


npm run build-uat
git pull
docker compose build --no-cache
docker compose up -d


⚙️ CÁC BƯỚC CHI TIẾT
🧱 1. Dừng container backend cũ
cd /root/projects/build-docker
docker-compose stop backend-spring

📦 2. Cập nhật code backend mới nhất
cd /root/projects/smart-java-spring-final
git pull origin main


Quay lại thư mục build-docker:

cd ../build-docker


Rồi rebuild toàn bộ:

docker compose build --no-cache

🚀 Bước 5: Khởi động lại hệ thống

Sau khi build xong, chạy:

docker compose up -d


Xem trạng thái:

docker ps

-----------------------------------------------------------------------------------------

🚀 CÁCH TRIỂN KHAI BUILD FRONTEND LOCAL → SERVER (Nginx Docker)
🧩 1. Build ở local

Tại thư mục dự án React/Vite trên máy bạn:

npm run build-uat


Khi xong, bạn sẽ có thư mục:

smart-react-vite-final/dist/


Đây là toàn bộ file tĩnh (HTML, CSS, JS, hình ảnh...) sẵn sàng để deploy.

cd ../smart-react-vite-final/

git pull

Bước 4. Restart lại nginx container
 cd ../build-docker/
docker-compose restart nginx

nên khi bạn ghi đè dist, Nginx sẽ tự đọc bản mới luôn.

-----------------------------------------------------------------------------------------
docker exec -it smart-db mysql -u root -p
Rồi trong MySQL prompt:

sql
Copy code
SET GLOBAL time_zone = '+07:00';
SET time_zone = '+07:00';


git init
git remote add origin https://scm.vnptlab.com/zalo-mini-app/cong-thong-tin-dien-tu-tinh-bac-ninh.git
git checkout -b dev
git add .
git commit -m "Khởi tạo dự án"
git push origin dev --force
